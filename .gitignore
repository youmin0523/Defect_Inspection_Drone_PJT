# Node & Environment Variables
node_modules/
.env

# MacOS & Windows
.DS_Store
Thumbs.db

# Claude Code
# - .claude/settings.json 및 .claude/hooks/ 는 팀 공용이므로 commit
# - 개인 Notion 싱크 / API 키 등 personal override 는 settings.local.json 에 담아 gitignore
.claude/settings.local.json

# Python 캐시
__pycache__/
*.pyc
*.pyo

# 업로드 파일 (프로필 이미지, 평면도 등 런타임 생성물)
backend/uploads/

# 녹화 파일
backend/recordings/

# Vibe Coding 자동화 (개인 로컬 파일)
screenshots/
.vibe_sync_cursor.json
sync_notion_logs.py
capture_result.py
update_screenshots.py
new_urls.json
output.txt
run.bat

tasks/

# ML 학습 산출물 (대용량)
backend/training/runs/
backend/training/datasets/
backend/training/eval/evaluation_results.json
*.onnx
*.onnx.data
*.npy
*.pt
!backend/models_weights/.gitkeep

# 학습 로그 (재생성 가능)
backend/training/*_log.txt

# 하자 점검 참고 영상 (대용량)
datasets/defect_videos/

# YOLO 학습 결과 (프로젝트 루트)
runs/

# Claude Code lock
.claude/scheduled_tasks.lock